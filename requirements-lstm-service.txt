fastapi>=0.110
pydantic>=2.0
uvicorn[standard]>=0.27
numpy>=1.26
requests>=2.31
streamlit>=1.32

# PyTorch（Windows 建议用官方 CPU 源安装，避免直接从 PyPI 装到不兼容的 wheel）
# 例子：
#   pip install torch==2.7.0+cpu --index-url https://download.pytorch.org/whl/cpu
